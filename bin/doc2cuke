#! /usr/bin/env ruby

require 'docbert'

ARGV.each do |file_name|
  raw = File.read(file_name)
  doc = Kramdown::Document.new(raw, :input => :docbert)
  out = file_name.sub('.md', '')

  IO.write out, doc.to_gherkin
end
